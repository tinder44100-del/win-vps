name: Tailscale Windows Connect with OAuth

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  connect:
    runs-on: windows-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Connect Tailscale باستخدام OAuth API token
      - name: Connect Tailscale via OAuth
        uses: tailscale/github-action@v4
        with:
          oauth_token: ${{ secrets.TAILSCALE_OAUTH_TOKEN }}  # ضع هنا توكنك في GitHub Secrets
          ping: "a06-de-monam.tailabd540.ts.net"            # الجهاز داخل Tailnet

      # 3️⃣ التحقق من الاتصال بالجهاز
      - name: Verify Tailscale Connection
        run: |
          echo "Checking connection to Tailnet host..."
          tailscale status
          tailscale ping a06-de-monam.tailabd540.ts.net

      # 4️⃣ إنشاء مستخدم RDP بصلاحيات المسؤول (اختياري)
      - name: Enable RDP
        run: |
          net user runneradmin Pass123! /add
          net localgroup administrators runneradmin /add
          echo "✅ RDP user created"

      # 5️⃣ تنفيذ أوامر PowerShell على VPS داخل Tailnet
      - name: Run commands on Tailnet host
        run: |
          echo "✅ Connected successfully to a06-de-monam.tailabd540.ts.net"
          # مثال: تشغيل أي أوامر PowerShell على الجهاز
          # يمكنك هنا إضافة أي script أو curl أو ssh حسب حاجتك
